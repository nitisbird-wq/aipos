Populated by librarian only — one file per topic (e.g. `company-x.md`), a standing position updated (never silently overwritten) as evidence changes. Empty on purpose until the first pipeline run completes.
